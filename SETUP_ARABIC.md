# دليل تشغيل مشروع HomeyChef باستخدام XAMPP

## المتطلبات الأولية
- ✅ XAMPP مثبت على جهازك
- ✅ Node.js مثبت (احدث النسخ)
- ✅ npm مثبت (مع Node.js)

---

## خطوات التشغيل:

### 1️⃣ تشغيل XAMPP
1. افتح XAMPP Control Panel
2. شغل Apache و **MySQL**
3. تأكد إن MySQL شغال (يكون باللون الأخضر)

### 2️⃣ إنشاء قاعدة البيانات
1. افتح http://localhost/phpmyadmin
2. اضغط على **New** أو "جديد" من الجانب الأيسر
3. اسم القاعدة: `homeychef`
4. Encoding: `utf8mb4_unicode_ci`
5. اضغط **Create**

### 3️⃣ إعداد ملف البيئة
1. افتح ملف `backend/.env.example`
2. انسخه باسم `backend/.env`
3. تأكد من الإعدادات التالية:
```
DB_NAME=homeychef
DB_USER=root
DB_PASSWORD=     (فارغ إذا ما عندك باسورد على XAMPP)
DB_HOST=localhost
PORT=3000
```

### 4️⃣ تثبيت المكتبات
افتح PowerShell أو CMD في مجلد المشروع ثم:
```bash
cd backend
npm install
```

### 5️⃣ تهيئة قاعدة البيانات
```bash
node init.js
```

### 6️⃣ تشغيل السيرفر
```bash
npm start
```

أو للتشغيل مع إعادة التشغيل التلقائي:
```bash
npm run dev
```

---

## تشغيل الفرونت إند
افتح الملفات التالية في المتصفح:

- 🏠 **الرئيسية**: `frontend/index.html`
- 🔐 **تسجيل دخول**: `frontend/login.html`
- 📝 **تسجيل**: `frontend/register.html`
- 🍽️ **القائمة**: `frontend/menu.html`

---

## معلومات تسجيل الدخول الافتراضية

### Admin (مشرف)
- 📧 **Email**: admin@homeychef.com
- 🔑 **Password**: admin123

---

## التحقق من التشغيل الصحيح

✅ السيرفر يعمل: يجب أن ترى في Terminal:
```
🚀 HomeyChef Server running on port 3000
📍 API available at http://localhost:3000/api
```

✅ قاعدة البيانات متصلة: يجب أن ترى:
```
✅ Database connection successful
✅ Database tables synced successfully
```

✅ اختبار API: افتح المتصفح في:
```
http://localhost:3000/api/health
```
يجب أن ترى: `{"status":"OK","message":"HomeyChef API is running"}`

---

## استكشاف الأخطاء

### مشكلة في الاتصال بقاعدة البيانات
- تأكد أن MySQL شغال في XAMPP
- تأكد من اسم قاعدة البيانات `homeychef` موجودة
- تأكد من الباسورد صحيح في ملف `.env`

### مشكلة Port مستخدم
- غير الـ PORT في ملف `.env` من 3000 لرقم آخر (مثل 3001)
- أو أغلق البرنامج اللي يستخدم الـ port 3000

### مشكلة في المكتبات
- احذف مجلد `node_modules` ومجلد `package-lock.json`
- ثم نفذ: `npm install`

---

## ملاحظات مهمة

1. **يجب أن يكون MySQL و Apache شغالين في XAMPP**
2. **ملف `.env` يجب يكون في مجلد `backend` وليس في root**
3. **لا تحذف ملف `.gitkeep` في مجلد `backend/uploads`**
4. **السيرفر مايتقفل ولا تغلق الـ Terminal**

---

## المساعدة

إذا واجهت أي مشكلة، تأكد من:
- ✅ MySQL شغال في XAMPP
- ✅ قاعدة البيانات `homeychef` موجودة
- ✅ ملف `.env` موجود وصحيح
- ✅ جميع المكتبات مثبتة (`npm install`)
- ✅ البورت 3000 متاح

