# 🚀 خطوات تشغيل مشروع HomeyChef مع XAMPP

## المتطلبات:
- ✅ XAMPP مثبت
- ✅ Node.js مثبت

---

## 📋 الخطوات بالترتيب:

### 1️⃣ افتح XAMPP Control Panel
- شغّل **Apache** و **MySQL**
- تأكد أن الاثنين باللون الأخضر ✅

### 2️⃣ أنشئ قاعدة البيانات
1. افتح المتصفح على: `http://localhost/phpmyadmin`
2. اضغط على تبويب **Databases** (قواعد البيانات)
3. اسم القاعدة: `homeychef`
4. Collation: اختر `utf8mb4_unicode_ci`
5. اضغط **Create** (إنشاء)

### 3️⃣ شغّل السيرفر (Backend)
افتح PowerShell أو CMD في مجلد المشروع:

```powershell
cd backend
node init.js
```

إذا نجح، سترى:
```
✅ Database connection successful
✅ Database tables synced successfully
✅ Default admin created
```

بعدين شغّل السيرفر:
```powershell
npm start
```

سوف ترى:
```
🚀 HomeyChef Server running on port 3000
📍 API available at http://localhost:3000/api
```

### 4️⃣ افتح الفرونت إند
افتح في المتصفح الملفات التالية:
- `frontend/index.html` - الصفحة الرئيسية
- `frontend/login.html` - تسجيل الدخول
- `frontend/register.html` - تسجيل مستخدم جديد

---

## 🔑 معلومات تسجيل الدخول:
- **Email**: admin@homeychef.com
- **Password**: admin123

---

## ⚠️ ملاحظات مهمة:

### إذا طلع Error في قاعدة البيانات:
1. تأكد إن MySQL في XAMPP شغال
2. تأكد إن قاعدة البيانات `homeychef` موجودة
3. إذا كان عندك باسورد على MySQL، عدّل ملف `backend/.env`:
   ```
   DB_PASSWORD=الباسورد_التاعك
   ```

### إذا ما في باسورد (الأكثر شيوعاً):
ملف `.env` يكون هيك:
```
DB_PASSWORD=
```
(فارغ بدون أي كلمة)

### إذا Port 3000 مشغل:
عدّل ملف `.env`:
```
PORT=3001
```
أو أي رقم ثاني

---

## 📁 هيك المشروع بعد التشغيل:

```
HomeyChef/
├── backend/
│   ├── .env          ✅ موجود
│   ├── server.js     ✅ شغال على port 3000
│   └── node_modules/ ✅ المكتبات مثبتة
├── frontend/         ✅ الملفات HTML موجودة
└── خطة_التشغيل.md   📋 هذا الملف
```

---

## ✅ التحقق من أن كل شي شغال:

1. **MySQL شغال؟** 
   - XAMPP Control Panel → MySQL باللون الأخضر ✅

2. **قاعدة البيانات موجودة؟**
   - phpMyAdmin → هناك قاعدة اسمها `homeychef` ✅

3. **السيرفر شغال؟**
   - افتح: http://localhost:3000/api/health
   - لازم يطلع: `{"status":"OK","message":"HomeyChef API is running"}` ✅

4. **الفرونت إند يعمل؟**
   - افتح `frontend/index.html` في المتصفح ✅

---

## 🎉 إذا كل شي طلع معك:
- السيرفر: يعمل ✅
- قاعدة البيانات: متصلة ✅
- الموقع: يفتح ✅

**مبروك! المشروع جاهز للاستخدام! 🚀**


